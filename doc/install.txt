Do whatever sections apply.

LINUX
  install from USB
  sudo apt-get gnome-desktop-environment

Git
  ## first, set up account on github (and get permissions from Abram)
  sudo apt-get install git
    git config --global user.name "xxx"
    git config --global user.email "xxx@gmail.com"
    git config --global credential.helper cache
    git config --global credential.helper 'cache --timeout=3600'
    # git clone https://github.com/hivedev/newhive.git
    git clone -b v2-community https://github.com/hivedev/newhive.git

Python library dependency dependencies (Ubuntu packages)
    sudo apt-get install python-pip gfortran libblas-dev liblapack-dev python-dev g++
    sudo apt-get install build-essential python-dev

Python library dependencies
    sudo pip install ez_setup
    sudo pip install werkzeug pymongo boto oauth2 oauth2client pil paramiko cssutils hsaudiotag
    sudo pip install lxml webassets urllib3 apiclient httplib2 pytz jinja2 simplejson
    sudo pip install envoy scipy webkit2png
    sudo pip install pandas brownie google-api-python-client # for analytics
    # for development
    sudo pip install statprof yappi

    pyes -- https://github.com/aparo/pyes
        werkzeug --- ??? what repo

JavaScript dependencies
    # ?? sudo apt-get install npm

    node:
        wget http://nodejs.org/dist/v0.10.12/node-v0.10.12.tar.gz
        tar zxf node-v0.10.12.tar.gz
        cd node-v0.10.12
        chmod -R o+rX .
        make
        sudo make install
    cram:
        sudo npm install -g bower@0.9.2
        sudo npm install -g cram

Application dependencies (Ubuntu packages)
    sudo apt-get install mongodb ruby-sass ruby-compass cutycapt libicu48 imagemagick
    # these are on abram's machine.  scp
    #elastic search -- http://www.elasticsearch.org/download/
    #    sudo dpkg -i elasticsearch-0.20.5.deb
    #    # might need fix dependencies: 
    #    sudo apt-get -f install 

To make Cutycapt:
    sudo apt-get install subversion libqt4-webkit libqt4-dev g++ xvfb
    svn co svn://svn.code.sf.net/p/cutycapt/code/ cutycapt
    cd cutycapt/CutyCapt
    make clean;qmake;make
    xvfb-run ./CutyCapt --url=http://www.example.org --out=example.png

Get the db
    scp dev-1.newhive.com:/tmp/2013* .
    tar -xvf 2013*
    mongorestore --drop -d test --dir tmp/hive

Get the server config
    scp dev-1.newhive.com:newhive/newhive/config/config.py newhive/newhive/config
    edit the config for your favorite port

Run the server
    <from an enlistment>
    ./server.py

