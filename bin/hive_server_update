#! /bin/bash
# Pull down latest v2-community,
# Restart the newhive web server
# Run under crontab as newduke

# Pull latest
cd /var/www/newhive
sudo chown www-data:dev -R .
sudo chmod g+rwX -R .
git pull
sudo chown www-data:dev -R .
sudo chmod g+rwX -R .
# restart server
bin/hive_server_deploy

echo updated at
date
echo
